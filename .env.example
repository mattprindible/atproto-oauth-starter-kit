# ===========================
# Environment Configuration
# ===========================
# Copy this file to .env and fill in your values

# Port the application will listen on
PORT=3000

# ===========================
# Public URL (REQUIRED)
# ===========================
# Must be accessible from the internet for Bluesky OAuth to work
# Include protocol (https://) and domain - NO trailing slash
#
# Development (ngrok/local tunnel):
# PUBLIC_URL=https://your-app-name.ngrok-free.app
#
# Production (your domain):
PUBLIC_URL=https://your-domain.com

# ===========================
# Cookie Secret (REQUIRED)
# ===========================
# Must be at least 32 characters long
# Generate a secure secret with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
COOKIE_SECRET=your-secure-random-secret-at-least-32-characters-long

# ===========================
# Storage Backend
# ===========================
# Option 1: Redis (recommended for production)
# Uncomment and set REDIS_URL to use Redis instead of SQLite
# Railway.app: Add Redis plugin, Railway auto-sets this variable
# Docker: Use redis://redis:6379 (if using docker-compose.yml)
# Local Redis: Use redis://localhost:6379
REDIS_URL=redis://localhost:6379

# Option 2: SQLite (default if REDIS_URL not set)
# No configuration needed - creates db.sqlite file automatically
# Good for development and low-traffic single-server deployments

# ===========================
# Cryptographic Keys
# ===========================
# Option 1: File-based (default)
# Run: npm run generate-keys
# Creates keys.json file (don't commit to git!)
#
# Option 2: Environment variable (recommended for containers/PaaS)
# Uncomment and paste your keys as a JSON string
# KEYS_JSON={"privateJwk":{"kty":"EC",...},"publicJwk":{"kty":"EC",...}}
